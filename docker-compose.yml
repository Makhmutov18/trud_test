version: "3.9"

services:
  monolith:
    build: .
    container_name: trud-monolith
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend/data:/app/data
